for (var x = 1; x <= 3; x = x + 1) {
  print("Outer loop x = " + x);

  print(x);
  for (var y = 1; y <= 3; y = y + 1) {
    if (y == 2) break;

    if (x == 2 && y == 3) break;

    print("  Inner y = " + y);
  }
}
