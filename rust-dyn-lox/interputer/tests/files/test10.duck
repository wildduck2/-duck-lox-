class Dog {
  init(name) {
    if (name == "") {
      return;  // Early return
    }
    this.name = name;
  }
}


class Dog {
  init(name) {
    return "oops";  // Resolver catches this as an error!
  }
}


fun greet() {
  print("Hello");
}

fun add(a, b) {
  return a + b;
}

class Dog {
  init(name) {
    this.name = name;
  }
}

var dog = Dog("Buddy");
print(dog.name);o




class Dog {
  init(name) {
    if (name == "") {
      print("No name!");
      return;
    }
    this.name = name;
  }
}

var dog1 = Dog("");
var dog2 = Dog("Max");
print(dog2.name);

class Dog {
  init(name) {
    this.name = name;
  }
}




var dog = Dog("Buddy");
var result = dog.init("Max");
print(result);  // Should print the instance, not nil
print(dog.name); // Should be "Max" now




class Foo {
  init() {
    print("Initializing");
  }
}

var foo = Foo();
print(foo.init());  // Should return the same instance




