class Dog {
  init(name, breed) {
    this.name = name;
    this.breed = breed;
    print("Creating dog: " + name);
  }
  
  static bark(name) {
    print(name + " says woof!");
  }
}

var dog = Dog("Buddy", "Golden Retriever");
Dog.bark("Buddy");
